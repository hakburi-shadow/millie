# AI_LOG

**사용 도구**: Claude Code (Opus)

## 활용 범위와 위임 단위

되돌리기 어려운 판단은 직접 하고, 그 판단을 따르는 작업만 맡깁니다.

| 직접 | AI에 위임 |
|---|---|
| 아키텍처 — MVI 채택, 계층 경계, 모듈 구성과 이름 | 인터페이스·프로토콜 선언 |
| 라이브러리·문법 채택 여부 — Alamofire·Tuist·TCA·CI 미사용, typed throws 사용 | 구현, UI 조립 |
| 화면 레이아웃 판단 — 열 개수, 스크롤 방향, 이미지 표시 방식 | 테스트 코드 |
| 과제 문서의 모순을 어느 쪽으로 해석할지 | 문서 초안, 이름 일괄 변경 같은 기계적 작업 |
| **산출물 검토와 방향 수정** | `Package.swift` 등 형태가 정해진 반복 코드 |

경계는 **"판단이냐 작성이냐"** 로 긋습니다. 무엇을 만들지, 어떤 구조로 갈지, 무엇을 쓰지 않을지를
정하는 일은 직접 하고, 정해진 것을 코드와 문서로 옮기는 일은 맡깁니다.

**한계를 하나 밝혀 둡니다.** 테스트 코드도 AI가 작성합니다. 확인 항목과 구현을 같은 도구가 쓰면
테스트가 구현이 하는 대로 따라 적을 위험이 있습니다. 이를 줄이기 위해 확인 항목은 구현이 아니라
**요구되는 동작에서 끌어내고**, 테스트가 통과한 뒤에는 **그 기능을 일부러 끊고 다시 돌려
실제로 실패하는지 확인합니다(V5).** 통과가 기능 덕분인지 우연인지는 이렇게 해야 갈립니다.
사람이 독립적으로 작성한 테스트만큼 강하지는 않다는 점은 인정합니다.

**맡긴 코드는 읽고 판단합니다(V1).** 읽지 않고 넘기면 위임이 아니라 방치이고, 설명할 수 없는
코드가 남습니다.

**위임 단위는 기능 하나이고, 커밋 하나입니다.** 무엇을 만들지 정하고, 인터페이스와 구현을 맡기고,
검증하고, 어긋난 것을 고친 뒤, 코드·테스트·이 문서를 **같은 커밋에** 넣습니다. 기록이 해당 코드와
같은 커밋에 있어야 작업하면서 남겼다는 것이 히스토리로 드러납니다. 마지막에 몰아 쓴 기록은
마지막 커밋에 통째로 추가된 모양으로 남아 금방 드러납니다.

## 검증 절차

맡긴 결과물마다 아래를 같은 순서로 확인합니다. 하나라도 실패하면 수정 절차로 넘어갑니다.

| | 무엇을 | 비고 |
|---|---|---|
| **V1** | 돌려보기 전에 코드를 직접 읽습니다 | 테스트를 통과했다는 것이 맞게 짰다는 뜻은 아닙니다 |
| **V2** | `swift test --package-path Packages/<모듈>` 출력을 확인합니다 | 돌려보지 않고 "통과할 것"이라고 쓰지 않습니다 |
| **V3** | 금지된 `import`를 일부러 넣고 빌드해 컴파일 에러를 확인합니다 | ⚠️ `.build` 흔적이 남아 있으면 막혀야 할 import가 그냥 통과합니다. `--scratch-path`로 깨끗한 경로에서 확인합니다 |
| **V4** | 바깥 사실은 원문·실제 응답으로 확인합니다 | 기억이나 추측을 근거로 쓰지 않습니다 |
| **V5** | 기능을 일부러 끊고 다시 돌려, 그 테스트가 실패하는지 확인합니다 | 통과가 기능 덕분인지 우연인지를 가릅니다. 작업 후반에 추가한 절차라 기록 11부터 적용됐습니다 |

## 수정 절차

고치기 전에 어느 쪽 문제인지 먼저 나눕니다. 이 단계를 건너뛰면 구현만 계속 고치면서 설계 잘못을
덮게 됩니다.

| 문제 | 무엇을 하는가 |
|---|---|
| **설계** — 요구사항·계층 규칙과 어긋남 | 방향을 직접 다시 정합니다. **다시 맡기지 않습니다** |
| **구현** — 방향은 맞는데 코드가 틀림 | 무엇이 틀렸는지 짚어서 다시 맡깁니다 |
| **검증 방법** — 확인 방법이 문제를 못 잡음 | 방법을 바꾸고 처음부터 다시 검증합니다 |

고친 뒤에는 **왜 그렇게 고쳤는지**를 아래에 적고, 코드로 막을 수 있으면 테스트로 고정합니다.

## 기록

| # | 무엇을 맡겼나 | 어떻게 요청했나 | 검증 | 어느 쪽 문제 → 어떻게 고쳤나 |
|---|---|---|---|---|
| 1 | `Core_Domain` 선언부 — `Photo`, `AppError`, `PhotoRepository` | 의존성 0인 도메인 계층에 모델·에러·저장소 계약을 선언하도록 요청했습니다. 저장 기술과 화면을 모르는 상태를 유지하는 것을 조건으로 걸었습니다 | **V1** 코드 검토 / **V2** `swift test` 3건 통과 / **V3** `import RealmSwift` 를 넣고 `--scratch-path` 로 깨끗한 빌드 → `no such module 'RealmSwift'` 확인 / **V4** TheCatAPI 를 직접 호출해 응답 형식과 id 중복 확인 | ① **설계** — 초안이 `AppError.message` 에 한국어 표시 문구를 넣어 도메인이 화면을 알게 되었습니다. 문구를 앱 타깃으로 넘기고, `isRetryable` 은 `isRecoverableByRetry` 로 바꿔 도메인 규칙임을 드러냈습니다<br>② **설계** — 무한 스크롤을 "다음 페이지 요청"으로 잡았으나, 실제 호출해 보니 `page` 가 동작하지 않고 매번 무작위 결과에 같은 id 가 섞여 나왔습니다. `loadNext` 에 `excludingIDs` 를 넣어 "중복을 걸러내며 이어붙이기"임을 계약에 드러냈습니다<br>③ **설계** — 모듈 이름이 `Core_Entity` 였으나 담긴 것의 절반이 엔티티가 아니었습니다(에러·계약). 내용에 맞게 `Core_Domain` 으로 바꿨습니다 |
| 2 | `Core_Networking` — `URLSession` 기반 전송 계층 구현 | *"환경에 따른 제한이 걸릴 수 있는 api"* 라는 조건을 근거로, 상태 코드와 연결 실패를 재시도 가능 여부에 따라 분류하도록 요청했습니다. Alamofire 를 쓰지 않을 것과 특정 API 를 모르는 상태를 유지할 것을 조건으로 걸었습니다 | **V1** 코드 검토 / **V2** `swift test` 14건 통과 — `URLProtocol` 스텁으로 429·5xx·4xx·오프라인·타임아웃·형식 오류를 실제 서버 없이 재현 / **V3** `import Core_Domain` 을 넣고 깨끗한 경로에서 빌드 → `no such module 'Core_Domain'` 확인 | ① **설계** — 초안이 `Endpoint` 안에 TheCatAPI 주소와 파라미터를 함께 두어 전송 계층이 특정 API 에 묶였습니다. 주소·파라미터를 `Core_Service` 로 옮겨 이 모듈의 **의존성을 0으로** 만들었습니다<br>② **구현** — `Retry-After` 를 정수로만 읽어, 명세상 허용되는 HTTP-date 형식이 오면 대기 시간을 조용히 잃었습니다. RFC 1123 해석을 추가하고 두 형식 모두 테스트로 고정했습니다<br>③ **구현** — 연결 실패를 `URLError` 통째로 `.transport` 에 넣었으나, 오프라인과 타임아웃은 화면에서 다르게 처리됩니다(전자는 저장된 데이터로, 후자는 재시도). 두 경우를 별도 case 로 분리했습니다 |
| 3 | `ThirdParty_Realm` — realm-swift 어댑터 모듈 | SDK 의존을 한 모듈에만 두고 다른 모듈은 그 모듈을 통해서만 접근하도록 구성을 요청했습니다 | **V1** 코드 검토 / **V2** `swift build` 성공, Swift 6 모드에서 동시성 경고 0건 / **V4** `Package.resolved` 확인 — realm-swift 20.0.5, realm-core 20.1.5 | **검증 방법** — 처음에는 의존 버전을 느슨하게 두었습니다. 확인해 보니 한 저장소에 두 갈래 태그가 함께 있어 버전을 적지 않으면 구 버전이 잡힙니다. `from: "20.0.5"` 로 명시하고, 해석 결과를 `Package.resolved` 로 확인하는 것을 절차에 넣었습니다 |
| 4 | `Core_Persistence` — Realm 저장소와 이미지 캐시 구현 | Realm 타입이 패키지 밖으로 나가지 않는 상태를 유지하면서, 메타데이터 저장과 이미지 바이너리 보관을 나눠 구현하도록 요청했습니다 | **V1** 코드 검토 / **V2** `swift test` 9건 통과, Swift 6 모드 동시성 경고 0건 / **V3** `import Core_Networking` 을 넣고 깨끗한 경로에서 빌드 → `no such module` 확인 | ① **구현** — 초안이 `Realm` 인스턴스를 actor 프로퍼티로 들고 있었습니다. actor 는 호출 사이에 같은 스레드를 보장하지 않는데 Realm 인스턴스는 스레드에 묶여 있어, 다른 스레드에서 접근하는 순간 깨집니다. 메서드마다 새로 여는 방식으로 바꿨습니다<br>② **설계** — 초안의 `ImageDiskCache` 가 없는 이미지를 직접 내려받으려 해 저장 계층이 네트워크를 알게 되었습니다. 이 모듈은 보관만 맡고, 없으면 `nil` 을 돌려주도록 좁혔습니다. 내려받아 채우는 일은 조합 계층으로 넘겼습니다<br>③ **구현** — 디스크 쓰기 실패를 오류로 올려 정상 동작이 실패로 처리됐습니다. 캐시를 채우지 못한 것뿐이고 이미지는 메모리에 이미 있으므로, 쓰기 실패는 삼키도록 바꾸고 그 이유를 주석에 남겼습니다 |
| 5 | `Core_Networking` — 이미지 바이너리 내려받기 추가 | 이미지는 응답에 들어 있던 완성된 주소로 바이트를 그대로 받아야 하는데 기존 `HTTPClient` 는 JSON 디코딩 전용이라, 별도 경로를 추가하도록 요청했습니다 | **V1** 코드 검토 / **V2** `swift test` 16건, **3회 연속** 실행해 간헐 실패가 아님을 확인 | **검증 방법** — 새 테스트를 별도 suite 로 두었더니 다른 테스트의 응답을 받아 실패했습니다. `.serialized` 는 한 suite 안에서만 순서를 보장하고 suite 끼리는 병렬로 돌기 때문에, 전역 스텁을 쓰는 테스트가 서로를 덮어쓴 것입니다. 스텁을 쓰는 테스트를 한 suite 로 모으고 그 이유를 주석에 남겼습니다. 처음 한 번 통과했다고 넘어갔으면 나중에 원인 모를 실패로 돌아왔을 문제입니다 |
| 6 | `Core_Service` — API 명세, 응답 변환, 저장소 구현 | 네트워크 우선·실패 시 저장분 대체 정책과, 이미지의 캐시 우선 정책을 각각 구현하도록 요청했습니다 | **V1** 코드 검토 / **V2** `swift test` 13건 통과 / **V3** `import ThirdParty_Realm` 을 넣고 깨끗한 경로에서 빌드 | ① **설계** — 구현체 이름을 `DefaultPhotoRepository` 로 두었습니다. `Default` 는 쓰임에 대한 말이지 동작에 대한 말이 아니라 정보가 없습니다. 정책을 이름에 담아 `NetworkFirstPhotoRepository` / `CacheFirstPhotoDataLoader` 로 바꾸니, 메타데이터와 이미지의 정책 방향이 반대라는 점이 이름만으로 드러납니다<br>② **검증 방법** — **경계가 컴파일러로 강제된다고 단정했는데 사실이 아니었습니다.** `Core_Service` 는 `ThirdParty_Realm` 을 선언하지 않았는데도 import 가 통과합니다. SPM 이 전이 의존을 열어 두기 때문입니다. 앞서 막혔던 모듈들은 의존성이 0이라 전이 경로가 없었을 뿐이었습니다. 실제 유출은 `PhotoObject` 가 `internal` 이라 막히지만, 강제 수단이 계층마다 다르다는 사실을 문서에 정확히 적기로 했습니다<br>③ **구현** — 테스트 대역에서 호출 횟수를 `NSLock` 으로 셌으나 Swift 6 의 async 안에서는 쓸 수 없습니다. actor 로 바꿔 격리를 언어에 맡겼습니다 |
| 7 | 목록 화면의 상태 계층 — `PhotoListState` · Reducer · Store | 상태를 한 덩어리로 두고 변경은 순수 함수 한 곳에서만 일어나도록, 이어서 불러오기의 중복 제거와 중복 요청 방지를 포함해 구현하도록 요청했습니다 | **V1** 코드 검토 / **V2** 앱 타깃 테스트 13건 통과 (Reducer 7, Store 6) | ① **구현** — `any PhotoRepository` 로 받았더니 `throws(AppError)` 가 `any Error` 로 지워져 컴파일이 실패했습니다. 존재 타입을 거치면 오류 타입이 보존되지 않습니다. 제네릭으로 받고 `do throws(AppError)` 로 명시해 해결했습니다. typed throws 를 고를 때 예상했던 제약이 실제로 나타난 지점입니다<br>② **검증 방법** — 테스트가 실패해 코드를 의심했으나, 결과 파일을 열어 보니 원인은 *"Failed to clone device... stuck in creation state"* 로 시뮬레이터 복제 실패였습니다. 실패 메시지만 보고 코드를 고쳤다면 멀쩡한 코드를 건드릴 뻔했습니다. 병렬 테스트를 끄고 재확인했습니다<br>③ **구현** — 이 타깃은 Xcode 기본값이 `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` 라 선언한 타입이 모두 화면 스레드에 묶입니다. 그래서 순수 데이터인 상태 타입까지 격리되어, 기본 인자에서 만들 때 경고가 났습니다. 상태·입력·결과와 Reducer 를 `nonisolated` 로 빼서 "화면 없이 그대로 호출할 수 있다"는 주석과 코드를 일치시켰습니다 |
| 8 | 목록 화면 — 격자 규격, 칸, 화면, 조립 지점 | 세로는 한 칸, 가로는 다섯 칸에 300×120 으로 그리되 가로에서는 좌우로도 스크롤되도록 요청했습니다. 방향 판단은 기기 방향이 아니라 실제로 그릴 크기로 하도록 조건을 걸었습니다 | **V1** 코드 검토 / **V2** 앱 타깃 테스트 19건 통과 (격자 규격 6건 추가) / 시뮬레이터에서 실행해 **세로 화면에 실제 데이터가 표시되는 것을 눈으로 확인** | **검증 방법** — 가로 화면은 자동으로 회전시키지 못해 눈으로 확인하지 못했습니다. 다섯 칸·300×120·격자 너비가 화면 폭을 넘는다는 것은 단위 테스트로 고정했지만, 실제로 좌우 스크롤이 되는지는 테스트가 답해 주지 못합니다. 확인하지 못한 부분을 확인한 것처럼 적지 않기 위해 여기 남기고, 수동 회전으로 따로 확인합니다 |
| 9 | 목록 화면 — 가로에서의 스크롤 방식 변경 | 목록 전체는 위아래로만 움직이고 좌우는 줄이 각자 맡도록 요청했습니다 | **V1** 코드 검토 / **V2** 앱 타깃 테스트 24건 통과 (줄 나누기·줄 id 5건 추가) / 시뮬레이터를 **가로로 돌려 눈으로 확인** — 한 줄만 밀고 나머지 줄은 제자리, 아래로 내려도 좌우 위치가 유지됨 | ① **설계** — 격자 하나를 두 방향으로 스크롤시켰더니 아래로 내리는 동안 좌우 위치까지 함께 밀려, 보던 자리를 놓치게 됩니다. 줄을 화면의 단위로 삼아 바깥은 세로만, 좌우는 줄마다 따로 맡도록 바꿨습니다<br>② **구현** — 다음 묶음을 부르는 기준이 마지막 *칸* 이었습니다. 가로에서는 줄의 마지막 칸이 화면 밖에 있어, 좌우로 끝까지 밀기 전에는 다음 묶음을 부르지 못합니다. 기준을 마지막 *줄* 로 바꿨습니다<br>③ **검증 방법** — 기록 8에서 못 했던 가로 확인입니다. 메뉴 항목을 바로 클릭하면 **성공했다는 값이 돌아오지만 화면은 그대로**였습니다. 상위 메뉴부터 차례로 연 뒤 클릭하고, 체크 표시를 따로 읽어 확인하니 회전했습니다. 호출이 성공했다는 응답을 결과 확인으로 삼으면 안 된다는 것을 다시 확인했습니다 |
| 10 | 상세 화면 — UIKit `ImageDetailViewController`, SwiftUI 와의 연결, 목록에서 열기 | 네비게이션 바에 id 라벨과 뒤로가기 버튼을 두고, 이미지는 바를 뺀 나머지를 비율을 유지한 채 채우며, 3배까지 확대되고, 뒤로 누르면 닫히도록 요청했습니다. 목록은 SwiftUI, 상세는 UIKit 으로 나눠 맡겼습니다 | **V1** 코드 검토 / **V2** 앱 타깃 테스트 31건 통과 (상세 화면 7건 추가) / 시뮬레이터에서 **칸을 눌러 열고, 핀치로 확대하고, 뒤로 눌러 닫히는 것까지 눈으로 확인** | ① **구현** — 칸을 만드는 곳이 가로·세로 두 군데로 갈려 있었습니다. 탭 처리를 붙였더니 가로에만 붙어, 세로에서는 눌러도 아무 일이 없었습니다. 칸을 만드는 곳을 한 군데로 모아 방향이 갈려도 같은 칸을 쓰게 했습니다<br>② **검증 방법** — 이때 테스트는 31건 전부 통과한 상태였습니다. 탭이 어디에 붙었는지는 화면을 띄워 눌러 보기 전에는 드러나지 않습니다. 실제로 눌러 보지 않았다면 **절반의 화면에서 상세가 열리지 않는 채로** 커밋했을 것입니다. 화면 작업은 테스트 통과를 완료 근거로 삼지 않고 직접 조작해 확인합니다 |
| 11 | 연결 감시와 자동 재시도 — `ConnectivityMonitor`, Store 의 연결 복구 처리 | 요구사항을 원문과 대조해 보니 Combine 이 `@Published` 한 곳뿐이라 *"비동기 처리에 썼다"* 고 보기 어려웠습니다. 끝없이 흘러오는 값(연결 상태)은 Combine, 요청 한 번은 Swift Concurrency 로 축을 나누도록 요청했습니다 | **V1** 코드 검토 / **V2** 앱 타깃 35건 + `Core_Networking` 17건 통과 / **구독을 일부러 끊고 돌려, "다시 불러온다" 테스트 2건이 실제로 실패하는 것을 확인** (통과가 기능 덕분인지 확인) / 앱을 실행해 회귀 없음 확인. 다만 **실제로 연결을 끊었다 붙이는 것은 시뮬레이터에서 재현하지 못해 그 구간은 확인하지 못했습니다** | ① **검증 방법** — 요구사항을 기억으로 판단해 "기능이 전부 끝났다"고 보고했습니다. PRD 원문과 코드를 한 줄씩 대조하니 Combine 의 자리, XCUITest, 문서 2종이 빠져 있었습니다. 완료 판단은 기억이 아니라 원문 대조로 합니다<br>② **구현** — 근거로 적어 둔 주석이 코드와 어긋났습니다. *"상태를 구독하는 쪽이 SwiftUI 목록과 UIKit 상세 화면 둘 다"* 라고 썼는데 상세 화면은 구독하지 않습니다. 주석도 검증 대상입니다<br>③ **구현** — Combine 의 `Subject` 가 Swift 6 의 스레드 검사와 부딪혀 컴파일이 막혔습니다. `@preconcurrency` 로 경고로 낮추는 대신, 값을 보내는 곳이 직렬 큐 하나뿐이라는 근거를 적고 그 범위만 포장했습니다<br>④ **검증 방법** — 테스트에서 Store 를 `_ =` 로 버렸더니 곧바로 해제되어 구독까지 사라집니다. 그러면 "아무 일도 일어나지 않는다" 테스트가 엉뚱한 이유로 통과합니다. 이름을 붙여 살려 두고 상태까지 확인하도록 고쳤습니다 |
| 12 | 화면 자동화 테스트 — UI 테스트 타깃 추가와 목록→상세 흐름 검증 | 선택 조건의 XCUITest 가 빠져 있었습니다. 목록이 뜨는지, 칸을 누르면 상세가 열리고 뒤로 누르면 닫히는지, 가로로 돌려도 목록이 남는지를 실제로 눌러 확인하도록 요청했습니다 | **V1** 코드 검토 / **V2** 앱 타깃 35건 + 화면 자동화 3건 통과 / **탭 연결을 일부러 끊고 돌려 "상세 화면이 열리지 않았습니다" 로 실패하는 것을 확인** / 프로젝트 파일을 손으로 고쳤으므로 `xcodebuild -list` 로 타깃 인식을 따로 확인 | ① **검증 방법** — 기록 10에서 눈으로 찾았던 버그(세로에서 탭이 안 되던 것)를 이 테스트가 잡습니다. 탭 연결을 끊고 돌리니 정확히 그 지점에서 실패했습니다. 사람이 한 번 확인하고 지나갔던 것을 다음부터는 자동으로 잡히게 바꿨습니다<br>② **구현** — 화면 요소 이름을 한 곳에 모았다고 주석에 적었는데, 화면 자동화 테스트는 앱과 **다른 프로세스**라 앱의 타입을 가져다 쓸 수 없습니다. 이름을 양쪽에 각각 적어야 하는 것이 사실이라, 주석을 그렇게 고쳤습니다. 기록 11 과 같은 종류의 실수가 반복됐습니다<br>③ **한계** — 이 테스트는 실제 서버에서 목록을 받아옵니다. 연결이 없거나 호출 제한에 걸리면 기능과 무관하게 실패합니다. 대역으로 바꾸려면 앱에 테스트용 통로를 내야 해서, 지금은 한계로 두고 대기 시간을 넉넉히 두었습니다 |
| 13 | 제출 문서 — `DESIGN.md`, `RETRO.md`, `README.md` 정리 | 설계 문서는 모듈 구조·의존성·상태 흐름·트레이드오프 네 가지만 다루고, 각 선택마다 **무엇을 포기했는지**를 함께 적도록 요청했습니다. 회고는 잘된 것보다 어긋났던 것을 구체적으로 적도록 했습니다 | **V1** 문서 검토 / **V4** 문서에 적은 사실을 코드와 대조 — 의존 관계, 테스트 수, 경계를 지키는 수단 | ① **구현** — `README` 에 *"계층 위반이 컴파일 에러가 되도록 했습니다"* 라고 적혀 있었습니다. 기록 6에서 이미 사실이 아님을 확인했는데 문서에는 그대로 남아 있었습니다. 확인한 내용이 문서까지 반영됐는지를 따로 보지 않으면 이렇게 남습니다<br>② **구현** — 이 문서의 절차 항목에 *"대응하는 요구사항 번호를 테스트에 주석으로 남깁니다"* 가 있었으나, 그 주석은 앞선 작업에서 전부 걷어냈습니다. **절차 문서가 실제 절차와 달랐습니다.** 지금 하고 있는 것(기능을 끊고 테스트가 실패하는지 확인)으로 바꿔 적고 V5 로 절차에 넣었습니다<br>③ **검증 방법** — 깨끗한 클론에서 전체 테스트를 돌린 출력을 `tail` 로 잘라 본 탓에, 화면 자동화 테스트가 빠진 것으로 잘못 읽었습니다. 잘린 출력으로 판단한 것이 원인이라, 지정 실행과 전체 실행을 각각 돌려 3건이 도는 것을 확인했습니다 |
