# RETRO

고양이 이미지 목록과 상세 화면을 만들었습니다. 목록은 SwiftUI, 상세는 UIKit 이고 상태는 MVI 로 모았습니다. 모듈은 로컬 SPM 패키지 다섯 개로 나눴습니다.

모듈을 나눌 때 "이 모듈이 무엇을 모르는가"를 기준으로 삼은 것과, 비동기를 요청 한 번짜리와 끝없이 흘러오는 것으로 갈라 쓴 것은 지금도 괜찮은 선택이었다고 봅니다. 아래는 그렇지 못했던 쪽입니다.

## 돌아보면

기능을 다 만들고 끝났다고 했는데, 요구사항 문서를 다시 펴서 한 줄씩 대조해 보니 세 가지가 빠져 있었습니다. Combine 을 제대로 쓸 자리, 화면 자동화 테스트, 문서 두 개. 머릿속의 요구사항과 문서의 요구사항이 달랐던 겁니다. 마지막에 발견해서 추가 커밋이 세 개 생겼습니다.

테스트를 믿었다가 놓칠 뻔한 것도 있습니다. 목록에서 칸을 만드는 코드가 가로용과 세로용으로 갈려 있었는데, 칸을 눌러 상세로 넘어가는 처리를 가로 쪽에만 붙였습니다. 그래서 세로에서는 칸을 눌러도 아무 일이 없었습니다. 기본이 세로 화면이니 사실상 상세를 못 여는 상태였는데, 그때 테스트는 31건 전부 통과 중이었습니다. 시뮬레이터에서 직접 눌러 보다가 알았습니다. 지금은 칸을 만드는 코드를 공통 함수로 추출해서, 가로든 세로든 같은 칸을 씁니다.

실패 원인을 중간에서 버리고 있던 것도 늦게 알았습니다. 네트워크가 실패하면 저장된 이미지로 대체하는데, 그때 "대체했다"는 사실만 넘기고 왜 실패했는지는 넘기지 않았습니다. 그래서 타임아웃이든 서버 오류든 호출 제한이든 화면에는 똑같이 "연결이 없어 저장된 이미지를 보여주고 있어요" 라고 떴습니다. 실패를 분류하는 쪽에도 문구를 고르는 쪽에도 테스트가 있었는데, 그 둘을 잇는 경로는 아무도 안 봤습니다.

검수하면서 더 곤란했던 것은, 주석에 확인하지 않은 것을 확인한 것처럼 적어 둔 대목이었습니다. "같은 id 가 반복 반환됩니다(실측)" 라고 써 놓고 실제로 재 본 적이 없었습니다. 뒤늦게 재 보니 100건에 1% 였습니다. 429 처리도 마찬가지로 명세만 보고 만든 것인데 그렇게 읽히지 않게 적어 뒀습니다. 지금은 확인한 것과 명세만 보고 쓴 것을 주석에서 구분해 두었습니다.

이름을 잘못 지은 것도 하나 있었습니다. `AppError.offlineAndEmpty` 는 "비어 있음"까지 이름으로 약속했는데, 오프라인에서 이어 불러오다 저장분이 떨어졌을 때도 이 값이 나왔습니다. 그래서 이미지가 가득한 화면에 "저장된 이미지도 없어요" 가 붙었습니다.

주석이 코드와 다른 경우도 두 번 나왔습니다. 둘 다 왜 그렇게 했는지를 적어 둔 문장이라 더 곤란했습니다.

## 다시 한다면

요구사항 대조를 마지막에 몰아서 하지 않겠습니다. 커밋 하나 끝낼 때 해당하는 조항만 확인하면 되는 일이었습니다.

화면 작업에는 자동화 테스트를 먼저 붙이겠습니다. 나중에 붙여 보니 앞에서 손으로 찾았던 버그를 그대로 잡아냈습니다. 시뮬레이터를 가로로 돌리는 것도 애플스크립트로 한참 헤맸는데, XCUITest 에서는 `XCUIDevice.shared.orientation` 한 줄이었습니다.

## 남은 것

- 연결이 끊겼다 붙는 상황과 실제 429 응답은 받아 보지 못했습니다. 원인이 화면까지 전달되는 것은 대역으로 확인했습니다.
- 화면 자동화 테스트가 실제 서버를 씁니다. 연결이 없으면 기능과 무관하게 실패합니다. 앱에 테스트 전용 통로를 내는 것보다는 낫다고 봤습니다.
- `Core_Service` 가 Realm 모듈을 직접 쓰는 것은 컴파일러가 막지 못합니다. 지금은 `internal` 로 실제 유출만 막고 있습니다.

---

AI 로 코드를 만드는 시간보다, 만들어진 게 맞는지 확인할 방법을 정하는 데 시간이 더 들었습니다. 위에 적은 것들은 전부 코드를 다시 읽거나 기능을 일부러 망가뜨려 보다가 나왔습니다. 테스트가 통과하는 것만 보고 있었으면 하나도 못 찾았을 겁니다.
