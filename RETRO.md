# RETRO

## 무엇을 만들었나

고양이 이미지 목록과 상세 화면입니다. 목록은 SwiftUI, 상세는 UIKit 으로 만들었고, 상태는 MVI 로 한 덩어리에 모았습니다. 모듈은 로컬 SPM 패키지 5개로 나눴습니다. 작업 커밋마다 그때의 AI 위임과 검증 기록을 `AI_LOG.md` 에 함께 남겼습니다.

## 잘 됐다고 보는 것

**모듈을 "무엇을 모르는가"로 나눈 것.** 의존성 0인 모듈을 두 개 만들어 두니 그 경계는 컴파일러가 지켜 줍니다. 다만 이것이 모든 경계에 해당하지는 않는다는 점도 확인해서 문서에 적었습니다.

**비동기를 성격으로 나눈 것.** 요청 한 번은 Swift Concurrency, 끝없이 흘러오는 것은 Combine 으로 나누니 두 도구를 억지로 섞지 않고도 각자 자리가 생겼습니다.

**틀린 것을 확인할 방법을 같이 만든 것.** 테스트를 쓴 뒤에는 기능을 일부러 끊고 돌려서 그 테스트가 실제로 실패하는지 확인했습니다. 통과가 기능 덕분인지 우연인지는 이렇게 해야 알 수 있습니다.

## 어긋났던 것

**"다 끝났다"를 기억으로 판단했습니다.** 기능 구현을 마치고 완료라고 보고했는데, 요구사항 원문과 코드를 한 줄씩 대조하니 Combine 의 자리, 화면 자동화 테스트, 문서 2종이 빠져 있었습니다. 완료 판단은 기억이 아니라 원문 대조로 해야 한다는 것을 늦게 배웠습니다.

**주석이 코드와 어긋난 일이 두 번 있었습니다.** 한 번은 "상태를 UIKit 상세 화면도 구독한다"고 적었는데 실제로는 구독하지 않았고, 한 번은 "화면 요소 이름을 한 곳에 모았다"고 적었는데 자동화 테스트는 다른 프로세스라 그럴 수 없었습니다. 둘 다 근거로 쓴 문장이라 더 나빴습니다. 주석도 검증 대상입니다.

**테스트가 통과해도 화면은 안 될 수 있었습니다.** 칸을 만드는 코드가 가로용·세로용 두 군데로 갈려 있어서, 탭 처리가 가로에만 붙었습니다. 그때 테스트는 31건 전부 통과 중이었습니다. 직접 눌러 보지 않았다면 절반의 화면에서 상세가 열리지 않는 채로 넘어갔을 것입니다.

**실패 메시지를 그대로 믿을 뻔했습니다.** 테스트가 실패해 코드를 의심했는데, 결과 파일을 열어 보니 원인은 시뮬레이터 복제 실패였습니다. 메시지만 보고 고쳤다면 멀쩡한 코드를 건드렸을 것입니다.

## 다시 한다면

**요구사항 대조를 마지막이 아니라 커밋마다 합니다.** 이번에는 전부 만든 뒤에 대조해서 되돌아가는 커밋이 세 개 생겼습니다. 각 커밋에서 해당 조항만 확인했다면 그 자리에서 끝났을 일입니다.

**화면 작업에는 자동화 테스트를 먼저 붙입니다.** 화면 자동화 테스트를 나중에 추가하고 보니, 앞서 손으로 찾았던 버그를 그대로 잡아냈습니다. 순서가 반대였다면 손으로 찾는 시간이 없었을 것입니다.

**주석에 "왜"를 적을 때는 그 문장도 코드로 확인합니다.** 근거를 적는 습관 자체는 유지하되, 적은 내용이 지금 코드와 맞는지 확인하는 단계를 붙이겠습니다.

## 남은 한계

- **연결이 실제로 끊겼다 붙는 상황을 확인하지 못했습니다.** 시뮬레이터에 네트워크를 끊는 수단이 없었습니다. 상태 계층이 신호를 받았을 때의 동작은 테스트로 고정했지만, 시스템이 신호를 제대로 주는지는 확인하지 못했습니다.
- **화면 자동화 테스트가 실제 서버에 의존합니다.** 연결이 없거나 호출 제한에 걸리면 기능과 무관하게 실패합니다.
- **`Core_Service` 가 Realm 모듈을 직접 쓰는 것은 컴파일러가 막지 못합니다.** 지금은 접근 제어로 실제 유출만 막고 있습니다.
- **가로에서 다섯 칸이 한눈에 들어오지 않습니다.** 화면 폭보다 줄이 넓어 오른쪽으로 밀어야 나머지가 나옵니다. 세로 이동과 가로 이동을 분리한 대가입니다.

## AI 를 쓰면서 남는 생각

코드를 만들어 내는 속도보다, **만들어진 것이 맞는지 확인하는 방법**을 정하는 데 시간이 더 들었습니다. 그리고 그 시간이 값어치를 했습니다. 이번에 걸러낸 것들 — 잘못된 주석, 반쪽만 붙은 탭 처리, 헛도는 테스트, 기억에 의존한 완료 판단 — 은 전부 코드를 다시 읽거나 일부러 망가뜨려 봐서 나왔지, 결과물이 그럴듯해 보이는지로는 나오지 않았습니다.
